schema_version: 1
kind: device_plan
id: ayaneo.pocket_air_mini.base
name: AYANEO Base Pocket Air Mini Setup
description: Starter setup for the AYANEO Pocket Air Mini.
device_profile_ref: ayaneo.pocket_air_mini
recipes:
  - recipe_ref: app.retroarch.provision
    selected_by_default: true
  - recipe_ref: feature.copy_bios
    selected_by_default: true
defaults:
  show_advanced_steps: false
overrides:
  # Note: config_variants is metadata only and does not affect binding resolution or emitted execution plans.
  config_variants:
    vendor_family: ayaneo
    screen_class: handheld_4_3
metadata:
  tags:
    - base_setup
    - recommended
