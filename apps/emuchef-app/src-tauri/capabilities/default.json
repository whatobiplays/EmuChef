{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the read-only EmuChef application.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
