{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window permissions for the read-only EmuChef config editor shell.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
